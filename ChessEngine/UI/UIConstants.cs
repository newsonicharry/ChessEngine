namespace ChessEngine.UI;

public abstract class UIConstants
{
    public const int Width = 1080;
    public const int Height = 720;
    public const int SideLength = 75;

    public const int InitialXOffset = Width / 2 - (SideLength * 8 / 2);
    public const int InitialYOffset = Height / 2 - (SideLength * 8 / 2);
}